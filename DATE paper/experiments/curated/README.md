Curated paper numbers live here as narrow CSV/JSON.

Do not edit digits by hand.  `scripts/aggregate_results.py` (Phase 13) is the only writer.
`run_experiment.py validate-curated` refuses archive-only, missing-hash, and unsigned runs.
