Post-synthesis timing-calibrated discrete-event model (Phase 5).

Calibration JSON produced by Router-primitive **MAXIMUM SDF / PT-PX** (DC ZeroWireload, not P&R) must be recorded under
`calibration/` with `model_version` and `calibration_hash`.  Formal 64/256/1024
matrix runs are forbidden until those hashes are locked.  Label every model
number `post-synthesis calibrated`; do not write post-layout.
